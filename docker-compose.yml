version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - backend/.env
    restart: unless-stopped
    volumes:
      - ./frontend:/app/frontend:ro
